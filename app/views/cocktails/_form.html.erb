<%= simple_form_for(@cocktail) do |f| %>
  <%= f.input :name, placeholder: "Cocktail name" %>
  <%= f.input :picture, placeholder: "image url" %>

  <%= f.button :submit, class: "btn btn-primary"  %>
<% end %>
